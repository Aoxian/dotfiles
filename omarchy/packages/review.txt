# Installed but not yet decided on. install.sh skips this file by default
# (see INSTALL_REVIEW in install.sh). Move a line to core.txt once you're sure,
# or delete it here if you don't want it.

# vim - unused; EDITOR is unset, nvim is the active editor.
# Disabled LazyVim config backup: ~/.config/nvim.disabled-lazyvim-20260826-232848
# (rename to ~/.config/nvim to restore, independent of this package)
vim
